// ==============================|| USER PROFILE TYPES ||============================== //

export interface UserProfile {
    id: number;
    name: string;
    email: string;
    contact: string;
    location: string;
}