// import { useTranslation } from 'react-i18next';

export function FormattedMessage({ id, values }: { id: string; values?: any }) {
  // const [trans] = useTranslation();
  return <>{/* trans(id, values) */}</>;
}
