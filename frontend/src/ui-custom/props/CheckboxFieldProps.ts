import BaseInputProps from './BaseInputProps';

export default class CheckboxFieldProps extends BaseInputProps {
    truthyValue?: string | boolean = true;
    falsyValue?: string | boolean = false;
}
